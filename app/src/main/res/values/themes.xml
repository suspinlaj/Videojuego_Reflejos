<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Videojuego" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Videojuego" parent="Base.Theme.Videojuego" />

    <!-- TITULO -->
    <style name="Titulo">
        <item name="android:textColor">@color/rojoOscuro</item>
        <item name="android:textSize">48sp</item>
        <item name="maxLines">1</item>
        <item name="fontFamily">@font/pixeltitulo</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- Texto Descriptivo -->
    <style name="TextoDescriptivo">
        <item name="android:textColor">@color/rojoOscuro</item>
        <item name="android:textSize">32sp</item>
        <item name="maxLines">1</item>
        <item name="fontFamily">@font/letra</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- DIALOG -->
    <style name="DialogoPixel" parent="ThemeOverlay.MaterialComponents.MaterialAlertDialog">
        <item name="backgroundTint">@android:color/transparent</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="materialAlertDialogTitleTextStyle">@style/DialogoTitulo</item>
        <item name="buttonBarPositiveButtonStyle">@style/DialogoBoton</item>
        <item name="buttonBarNegativeButtonStyle">@style/DialogoBoton</item>
    </style>

    <style name="DialogoTitulo">
        <item name="android:fontFamily">@font/pixeltitulo</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/rojo</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="DialogoBoton" >
        <item name="android:fontFamily">@font/pixeltitulo</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">10sp</item>
        <item name="android:textColor">@color/rojoClaro</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="DialogoEntry">
        <item name="android:background">@color/white</item>
        <item name="android:textColor">@color/rojoOscuro</item>
        <item name="android:maxLength">8</item>
        <item name="android:textSize">30sp</item>
        <item name="android:fontFamily">@font/letra</item>
        <item name="android:inputType">textVisiblePassword|textNoSuggestions</item>
    </style>


</resources>